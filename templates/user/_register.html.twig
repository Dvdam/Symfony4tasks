{% extends 'base.html.twig' %}

{% block title %}Registro de Usuarios
{% endblock %}
{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('assets/css/registro.css') }}" rel="stylesheet"/>
{% endblock %}
{% block body %}

<div class="row">
    <div class="col s12 l4 offset-l4">
        <div class="card hoverable">
            <dvi class="card-action">
                <h3>Registro de Usuarios</h3>
            </dvi>
            <div class="card-content">
                {{ form_start(form)}}
                {{ form_widget(form)}}
                {{ form_end(form)}}
            </div>
        </div>
    </div>
</div>


{% endblock %}
